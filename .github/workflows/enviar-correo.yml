name: Enviar notificación por correo

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  send-email:
    runs-on: ubuntu-latest
    if: |
      contains(github.event.issue.labels.*.name, '📦 Recibido') || 
      contains(github.event.issue.labels.*.name, '🚚 Envío') || 
      contains(github.event.issue.labels.*.name, '✅ Entregado')
    
    steps:
      - name: Esperar a que GitHub Pages se actualice
        run: sleep 180
      
      - name: Enviar correo
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: |
            ${{ github.event_name == 'issue_comment' && '📝 Actualización de tu pedido' ||
               contains(github.event.issue.labels.*.name, '📦 Recibido') && '📦 Tu pedido ha sido recibido' ||
               contains(github.event.issue.labels.*.name, '🚚 Envío') && '🚚 Tu pedido está en camino' ||
               contains(github.event.issue.labels.*.name, '✅ Entregado') && '✅ Tu pedido fue entregado' }}
          to: stithvalle14@gmail.com
          from: Maison Lumien Facturación
          html_body: |
            <!DOCTYPE html>
            <html>
            <head>
                <meta charset="UTF-8">
                <style>
                    body { font-family: 'Inter', sans-serif; background: #f5f5f5; padding: 20px; }
                    .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 16px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
                    .header { background: linear-gradient(135deg, #4F46E5, #10B981); padding: 40px; text-align: center; }
                    .header h1 { color: white; margin: 0; font-size: 28px; font-weight: 300; }
                    .content { padding: 40px; }
                    .button { display: inline-block; background: linear-gradient(135deg, #4F46E5, #3730A3); color: white; text-decoration: none; padding: 15px 30px; border-radius: 8px; margin: 20px 0; }
                    .comentario { background: #f0f0f0; padding: 20px; border-radius: 8px; border-left: 4px solid #4F46E5; margin: 20px 0; white-space: pre-wrap; }
                </style>
            </head>
            <body>
                <div class="container">
                    <div class="header">
                        <h1>MAISON LUMIEN</h1>
                        <p>El arte de vivir con luz</p>
                    </div>
                    <div class="content">
                        <h2>Hola,</h2>
                        
                        ${{ github.event_name == 'issue_comment' && 
                           '<p>Tu pedido tiene una nueva actualización:</p>' ||
                           contains(github.event.issue.labels.*.name, '📦 Recibido') && '<p>Hemos recibido tu producto en nuestras oficinas. Pronto lo enviaremos a tu dirección.</p>' ||
                           contains(github.event.issue.labels.*.name, '🚚 Envío') && '<p>¡Buenas noticias! Tu producto ya fue enviado a tu dirección.</p>' ||
                           contains(github.event.issue.labels.*.name, '✅ Entregado') && '<p>Tu producto ha sido entregado exitosamente. Esperamos que lo disfrutes.</p>' }}
                        
                        ${{ github.event_name == 'issue_comment' && 
                           format('<div class="comentario"><p>{0}</p></div>', github.event.comment.body) ||
                           '' }}
                        
                        <p><strong>Número de seguimiento:</strong> ${{ github.event.issue.title }}</p>
                        
                        <p>Puedes consultar el estado de tu pedido aquí:</p>
                        
                        <p style="text-align: center;">
                            <a href="https://maisonlumien.github.io/maison-lumien-seguimiento/seguimiento.html?track=${{ github.event.issue.title }}" class="button">
                                CONSULTAR MI PEDIDO
                            </a>
                        </p>
                        
                        <p>¿Tienes dudas? Escríbenos por WhatsApp:</p>
                        <p style="text-align: center;">
                            <a href="https://wa.me/573024157799" style="color: #25D366; text-decoration: none;">📱 302 415 7799</a>
                        </p>
                    </div>
                </div>
            </body>
            </html>
