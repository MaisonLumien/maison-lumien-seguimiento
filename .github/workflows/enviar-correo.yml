name: Enviar correo de prueba

on:
  issues:
    types: [opened, edited]

jobs:
  send-test-email:
    runs-on: ubuntu-latest
    if: contains(github.event.issue.labels.*.name, '📦 Recibido')
    
    steps:
      - name: Enviar correo
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: 📦 Tu pedido ha sido recibido (Prueba)
          to: stithvalle14@gmail.com
          from: Maison Lumien Facturación
          body: |
            Hola Cliente de Prueba,
            
            Hemos recibido tu producto en nuestras oficinas.
            
            Número de seguimiento: ${{ github.event.issue.title }}
            
            Consulta tu pedido: https://maisonlumien.github.io/maison-lumien-seguimiento/seguimiento.html?track=${{ github.event.issue.title }}
            
            Saludos,
            Maison Lumien
