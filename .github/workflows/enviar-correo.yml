name: Enviar notificación por correo

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  send-email:
    runs-on: ubuntu-latest
    if: |
      contains(github.event.issue.labels.*.name, '📦 Recibido') || 
      contains(github.event.issue.labels.*.name, '🚚 Envío') || 
      contains(github.event.issue.labels.*.name, '✅ Entregado')
    
    steps:
      - name: Extraer email del JSON
        id: extract
        run: |
          # Guardar el body del Issue en un archivo
          cat > issue_body.json << 'EOF'
          ${{ github.event.issue.body }}
          EOF
          
          # Usar Python para extraer el email del JSON
          python3 << 'PYTHON_EOF'
          import json
          import os
          
          try:
              # Leer el archivo JSON
              with open('issue_body.json', 'r') as f:
                  content = f.read()
              
              # Limpiar y parsear el JSON
              content = content.strip()
              data = json.loads(content)
              
              # Extraer email del campo cliente
              email = data.get('cliente', {}).get('email')
              nombre = data.get('cliente', {}).get('nombre', 'Cliente')
              tracking = '${{ github.event.issue.title }}'
              
              # Verificar que el email existe
              if not email:
                  raise Exception("No se encontró el campo 'email' en el JSON")
              
              print(f"✅ Email extraído: {email}")
              
              # Guardar en GITHUB_ENV
              with open(os.environ['GITHUB_ENV'], 'a') as env_file:
                  env_file.write(f"EMAIL={email}\n")
                  env_file.write(f"NOMBRE={nombre}\n")
                  env_file.write(f"TRACKING={tracking}\n")
                  
                  # Determinar estado por la etiqueta
                  if '📦 Recibido' in '${{ github.event.issue.labels.*.name }}':
                      env_file.write("ESTADO=recibido\n")
                  elif '🚚 Envío' in '${{ github.event.issue.labels.*.name }}':
                      env_file.write("ESTADO=enviado\n")
                  elif '✅ Entregado' in '${{ github.event.issue.labels.*.name }}':
                      env_file.write("ESTADO=entregado\n")
                  
                  # Verificar si es un comentario
                  if '${{ github.event_name }}' == 'issue_comment':
                      env_file.write("TIPO_EVENTO=comentario\n")
                      comentario = '''${{ github.event.comment.body }}'''
                      env_file.write(f"COMENTARIO={comentario}\n")
                      env_file.write(f"COMENTARIO_CORTO={comentario[:50]}...\n")
                  else:
                      env_file.write("TIPO_EVENTO=issue\n")
                      
          except Exception as e:
              print(f"❌ Error: {e}")
              # Si falla, NO envía correo
              exit(1)
          PYTHON_EOF
      
      - name: Esperar a que GitHub Pages se actualice
        run: sleep 180
      
      - name: Enviar correo
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: |
            ${{ env.TIPO_EVENTO == 'comentario' && '📝 Actualización de tu pedido' ||
               env.ESTADO == 'recibido' && '📦 Tu pedido ha sido recibido' ||
               env.ESTADO == 'enviado' && '🚚 Tu pedido está en camino' ||
               env.ESTADO == 'entregado' && '✅ Tu pedido fue entregado' }}
          to: ${{ env.EMAIL }}
          from: Maison Lumien Facturación
          html_body: |
            <!DOCTYPE html>
            <html>
            <head>
                <meta charset="UTF-8">
                <style>
                    body { font-family: 'Inter', sans-serif; background: #f5f5f5; padding: 20px; }
                    .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 16px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
                    .header { background: linear-gradient(135deg, #4F46E5, #10B981); padding: 40px; text-align: center; }
                    .header h1 { color: white; margin: 0; font-size: 28px; font-weight: 300; }
                    .header p { color: rgba(255,255,255,0.9); margin-top: 10px; }
                    .content { padding: 40px; }
                    .button { display: inline-block; background: linear-gradient(135deg, #4F46E5, #3730A3); color: white; text-decoration: none; padding: 15px 30px; border-radius: 8px; margin: 20px 0; }
                    .comentario { background: #f0f0f0; padding: 20px; border-radius: 8px; border-left: 4px solid #4F46E5; margin: 20px 0; white-space: pre-wrap; }
                </style>
            </head>
            <body>
                <div class="container">
                    <div class="header">
                        <h1>MAISON LUMIEN</h1>
                        <p>El arte de vivir con luz</p>
                    </div>
                    <div class="content">
                        <h2 style="color: #1F2937;">Hola ${{ env.NOMBRE }},</h2>
                        
                        ${{ env.TIPO_EVENTO == 'comentario' && 
                           '<p>Tu pedido tiene una nueva actualización:</p>' ||
                           env.ESTADO == 'recibido' && '<p>Hemos recibido tu producto en nuestras oficinas. Pronto lo enviaremos a tu dirección.</p>' ||
                           env.ESTADO == 'enviado' && '<p>¡Buenas noticias! Tu producto ya fue enviado a tu dirección.</p>' ||
                           env.ESTADO == 'entregado' && '<p>Tu producto ha sido entregado exitosamente. Esperamos que lo disfrutes.</p>' }}
                        
                        ${{ env.TIPO_EVENTO == 'comentario' && 
                           format('<div class="comentario"><p>{0}</p></div>', env.COMENTARIO) ||
                           '' }}
                        
                        <p><strong>Número de seguimiento:</strong> ${{ env.TRACKING }}</p>
                        
                        <p>Puedes consultar el estado de tu pedido aquí:</p>
                        
                        <p style="text-align: center;">
                            <a href="https://maisonlumien.github.io/maison-lumien-seguimiento/seguimiento.html?track=${{ env.TRACKING }}" class="button">
                                CONSULTAR MI PEDIDO
                            </a>
                        </p>
                        
                        <p>¿Tienes dudas? Escríbenos por WhatsApp:</p>
                        <p style="text-align: center;">
                            <a href="https://wa.me/573024157799" style="color: #25D366; text-decoration: none;">📱 302 415 7799</a>
                        </p>
                    </div>
                </div>
            </body>
            </html>
